# PASO 1
# Scrip para la configuración previa a la instalación del dominio.

$nameServer="X"
$addressIP="xxx.xxx.xxx.xxx"
$networkInternal="Ethernet X"
Rename-Computer -NewName $nameServer
Get-NetAdapter –name $networkInternal | Remove-NetIPAddress -Confirm:$false
Get-NetAdapter –name $networkInternal | New-NetIPAddress –addressfamily IPv4 –ipaddress $addressIP –prefixlength 24 –type unicast
Restart-Computer -force

# Explicación de que hace cada parte del script.
# $nameServer="X" Introducir el nombre del servidor.
# $addressIP="xxx.xxx.xxx.xxx" Introducir la dirección IP de la red interna
# $networkInternal="Ethernet X" Introducir el adaptador de red interna
# Rename-Computer -NewName $nameServer cambia el nombre del servidor
# Get-NetAdapter –name $networkInternal | Remove-NetIPAddress -Confirm:$false y Get-NetAdapter –name $networkInternal | New-NetIPAddress –addressfamily IPv4 –ipaddress $addressIP –prefixlength 24 –type unicast  Nos permiten añadir la IP de nuestra red interna
# Restart-Computer -force  Reinicia el sistema para que se apliquen los cambios.


# PASO 2

Get-Windowsfeature

# Comando para buscar que características del sistema hay y en que estado se encuentran.

Install-WindowsFeature -name AD-Domain-Services,DNS -IncludeManagementTools 

# Comando para instalar la caracteristica AD-Domain-Services y/o el DNS.
